/**
 * Figma alignment — when you share the file URL:
 * 1. Map color variables → `palette` / `paletteDark` in colors.ts
 * 2. Map number variables / grid → `spacing`, `radii` in spacing.ts
 * 3. Map text styles → `typography` in typography.ts
 * 4. Match components in src/components to Figma component specs (sizes, radii, states)
 */

export {};
